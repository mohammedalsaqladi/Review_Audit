# تمام | نظام المراجعة والتدقيق

تصميم أولي (Prototype) + مخطط قاعدة بيانات + سيرفر بسيط لمنصة مراجعة وتدقيق متعددة المكاتب (Multi-tenant).

## حالة المشروع الحالية

- الواجهة (public/index.html) تغطي تدفّق الاستخدام كاملاً: شاشة الأنظمة → تسجيل الدخول/إنشاء حساب → لوحة التحكم بكل أقسامها.
- شاشتا **دليل الحسابات** و**أوراق العمل** مربوطتان فعليًا بـ Supabase (قراءة وكتابة حقيقية) عبر السيرفر — بقية الشاشات (العملاء، الدردشات، العقود...) لا تزال تصميمًا تجريبيًا وتحتاج نفس نمط الربط لاحقًا.
- `database/schema.sql` مخطط قاعدة بيانات جاهز للتنفيذ مباشرة على Supabase (أو أي PostgreSQL)، يغطي: الشركات (المكاتب)، الأدوار، المستخدمين، الفروع، العملاء وملفاتهم وموظفيهم، ملفات التدقيق وموازين المراجعة، دليل الحسابات، أوراق العمل ومطابقتها التلقائية بالميزان، نقاط المراجعة، النماذج الجاهزة، العروض والعقود، الدردشات، الإشعارات، وسجل تدقيق العمليات.
- `database/seed-data-tamam.xlsx` بيانات تجريبية مترابطة لكل الجداول أعلاه، لتجربة الاستيراد ورؤية النتيجة.

## هيكل المشروع

```
tamam-audit-system/
├── server.js                 ← خادم Node/Express: تسجيل الدخول متعدد المكاتب + API دليل الحسابات وأوراق العمل
├── package.json
├── scripts/
│   └── hash-password.js       ← أداة لتوليد كلمة مرور مشفّرة (bcrypt) لإضافة مستخدم يدويًا
├── public/
│   └── index.html             ← الواجهة الكاملة (المصدر الذي يخدمه السيرفر)
├── database/
│   ├── schema.sql              ← مخطط قاعدة البيانات الكامل (PostgreSQL / متوافق مع Supabase)
│   └── seed-data-tamam.xlsx    ← بيانات تجريبية مترابطة لكل الجداول، لتجربة الاستيراد
└── README.md
```

## تشغيل السيرفر محليًا (اختياري، للتجربة قبل الرفع)

```bash
npm install
SUPABASE_URL="https://xxxxx.supabase.co" SUPABASE_SERVICE_ROLE_KEY="..." SESSION_SECRET="أي-نص-عشوائي-طويل" npm start
```

يفتح على `http://localhost:3000` — بدون تعبئة المتغيرات يشتغل السيرفر لكن أي طلب تسجيل دخول أو بيانات حقيقية يرجّع رسالة واضحة إنه غير متصل بقاعدة البيانات بعد.

## تعدد المكاتب (Multi-tenant) — كيف يعمل فعليًا

نشر واحد على Render يخدم **عددًا غير محدود من المكاتب** بنفس الرابط. لا يوجد أي متغير بيئة اسمه "COMPANY_ID" بعد الآن — كل مستخدم يسجّل دخوله برمز مكتبه (مثل `ETQAN`) + اسم المستخدم + كلمة المرور، والسيرفر يحدّد شركته من قاعدة البيانات في تلك اللحظة، ويصدر له جلسة (JWT) تحمل `company_id` الصحيح لبياناته هو فقط. مكتب ثانٍ يسجّل بنفس الرابط برمز مختلف (مثلاً `NOOR`) ويشتغل بمعزل كامل عن بيانات المكتب الأول.

لإضافة مكتب جديد: إمّا عبر شاشة "إنشاء حساب جديد" في الواجهة نفسها (تنشئ الشركة وأول مستخدم شريك فيها تلقائيًا)، أو يدويًا بإضافة صف في جدول `companies` + مستخدم في جدول `users` (استخدم `scripts/hash-password.js` لتشفير كلمة مروره أولًا).

## نشر المشروع على Render كـ **Web Service**

1. **New +** → **Web Service**، واربطه بمستودع GitHub الخاص بكم.
2. عبّئ الحقول:

| الحقل | القيمة |
|---|---|
| Branch | `main` |
| Root Directory | اتركه فاضيًا |
| Build Command | `npm install` |
| Start Command | `npm start` |

3. من قسم **Environment Variables** أضف:

| Name | Value | ملاحظة |
|---|---|---|
| `SUPABASE_URL` | رابط مشروعكم في Supabase | Project Settings → API |
| `SUPABASE_SERVICE_ROLE_KEY` | مفتاح `service_role` (وليس `anon`) | **سرّي تمامًا** — يبقى بالسيرفر فقط ولا يصل للمتصفح إطلاقًا |
| `SESSION_SECRET` | نص عشوائي طويل من اختياركم | يُستخدم لتوقيع جلسات الدخول (JWT)، غيّروه لقيمة قوية قبل الإنتاج |

4. اضغط **Create Web Service**. بعدها الرابط الناتج (مثل `tamam-audit-system.onrender.com`) يعمل مباشرة، وكل مكتب يسجّل دخوله برمزه الخاص من نفس الرابط.

> **لماذا `service_role` لا `anon`؟** لأن المتصفح لم يعد يتصل بـ Supabase مباشرة إطلاقًا — كل الطلبات تمر عبر `server.js` الذي يتحقق من الجلسة أولًا ثم يستعلم من Supabase نيابة عن المستخدم، ويفرض `company_id` الصحيح على كل عملية قراءة أو كتابة يدويًا. هذا أأمن من الاعتماد على مفتاح `anon` من المتصفح مباشرة.

## تنفيذ قاعدة البيانات على Supabase

1. أنشئ مشروعًا جديدًا في [supabase.com](https://supabase.com).
2. من لوحة التحكم افتح **SQL Editor**.
3. الصق محتوى `database/schema.sql` بالكامل ونفّذه (Run).
4. تحقّق من تبويب **Table Editor** لرؤية الجداول الناتجة.
5. أسهل طريقة لإنشاء أول مكتب ومستخدم: افتح الموقع بعد نشره واستخدم شاشة **"إنشاء حساب جديد"** — تُنشئ الشركة وأول مستخدم شريك تلقائيًا بكلمة مرور مشفّرة بشكل صحيح.
   بديلًا (يدويًا): أضف صفًا في `companies`، ثم صفًا في `users`، وشفّر كلمة المرور أولًا بـ `node scripts/hash-password.js "كلمة_المرور"` والصق الناتج في `password_hash`.
6. عند الحاجة لعزل بيانات كل مكتب تلقائيًا (Row Level Security) كطبقة حماية إضافية، الجداول التالية مفعّلة عليها RLS مسبقًا في الملف: `chart_of_accounts`, `wp_groups`, `wp_main_items`, `wp_sub_items`. لاحظ أن هذه السياسات مبنية على JWT من Supabase Auth، والنظام حاليًا لا يستخدم Supabase Auth (له تسجيل دخول خاص عبر `server.js`)، فهذه السياسات لا تؤثر على السيرفر لأنه يتصل بمفتاح `service_role` الذي يتجاوز RLS دائمًا؛ العزل الفعلي بين المكاتب مطبَّق داخل كود `server.js` نفسه (كل استعلام يُقيَّد بـ `company_id` من الجلسة).

## ملاحظة حول ربط دليل الحسابات وأوراق العمل بـ Supabase

لا حاجة لتعديل أي كود يدويًا بعد الآن. بمجرد ضبط متغيرات البيئة الثلاثة (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `COMPANY_ID`) على السيرفر — محليًا أو على Render كما بالأعلى — تُحمَّل شجرتا **دليل الحسابات** و**أوراق العمل** تلقائيًا من Supabase عند تسجيل الدخول، وأزرار "حفظ" في نوافذ الإضافة (حساب / مجموعة / رئيسي / فرعي) تكتب فعليًا في القاعدة. بدون ضبطها، تعمل الشاشتان بالبيانات التجريبية الثابتة كما هي.

## رفع المشروع على GitHub

هذا الحساب لا يملك صلاحية الاتصال بحساب GitHub الخاص بك مباشرة، لذلك الرفع يتم من جهازك بأربعة أوامر:

```bash
cd tamam-audit-system
git init
git add .
git commit -m "النسخة الأولى: تصميم الواجهة + مخطط قاعدة البيانات"

# أنشئ مستودعًا فارغًا في GitHub باسم مثلاً tamam-audit-system ثم:
git branch -M main
git remote add origin https://github.com/<اسم-حسابك>/tamam-audit-system.git
git push -u origin main
```

## الخطوات التالية المقترحة

- تعميم نمط الربط الحقيقي (المستخدم حاليًا في تسجيل الدخول، دليل الحسابات، وأوراق العمل) على بقية الشاشات: العملاء، الملفات، نقاط المراجعة، العروض والعقود، الدردشات — بنفس أسلوب إضافة مسارات `/api/...` في `server.js`.
- استخدام `fn_is_company_active()` من `schema.sql` داخل `/api/login` بدل التحقق اليدوي المكرر حاليًا في `server.js` (نفس المنطق، لتفادي الازدواجية).
- إضافة "نسيت كلمة المرور" فعليًا (إرسال بريد بريط إعادة تعيين).
- تفعيل RLS على بقية الجداول متعددة المستأجرين بنفس نمط `chart_of_accounts` و`wp_groups` كطبقة حماية إضافية (اختياري، لأن العزل الأساسي مطبَّق حاليًا داخل `server.js`).


## وحدة تقارير المراجعة (جديدة)

### خطوات التفعيل على قاعدة البيانات (مرة واحدة)

نفّذ الملفين بالترتيب في **Supabase → SQL Editor**:

1. `database/migration-2026-08-03-reports.sql` — ينشئ الجداول:
   - `audit_report_config` — قاعدة تهيئة تقارير المراجعة (نصوص الرأي/أساس الرأي/المسؤوليات/المتطلبات النظامية).
   - `audit_reports` — التقارير المُنشأة لكل عميل مع فقراتها (`sections` بصيغة JSONB).
   - `audit_report_events` — سجل الاعتماد وإلغاء الاعتماد (أثر تدقيقي).
   - يضيف على `companies` أعمدة: `stamp_url`, `signature_url`, `report_footer_ar`, `public_base_url`.
2. `database/seed-report-config.sql` — يعبّئ **164 نصًا معتمدًا** (المكتبة العامة) مأخوذة من ملف «شكل قاعدة تهيئة تقارير المراجعة».

> المكتبة العامة (`company_id = NULL`) مشتركة بين كل المكاتب. عند تعديل أي بند عام من واجهة المكتب يُنشأ **نسخة خاصة بالمكتب** تلقائيًا ولا تتأثر بقية المكاتب.

### الشاشات

| الشاشة | المسار في الشريط الجانبي |
|---|---|
| تهيئة تقارير المراجعة | تهيئة النظام ← تهيئة تقارير المراجعة |
| تقارير المراجعة | التقارير ← تقارير المراجعة |

**تهيئة تقارير المراجعة:** فلترة بنوع التقرير (سنوي / مرحلي) ونوع الرأي (غير معدل / متحفظ / معارض / امتناع)، شجرة مجموعات على اليمين بترتيب ظهورها في التقرير، وبحث نصّي. لكل بند: تعديل، تعطيل، حذف، وإنشاء نسخة «غير موحّدة». ويوجد استيراد جماعي باللصق من إكسل (المجموعة ⇥ البند ⇥ البيان ⇥ الكود).

**إنشاء تقرير:** اختيار العميل ← ملف العميل ← سنوي/مرحلي ← موحّدة/غير موحّدة ← نوع الرأي. عندها يُركّب النظام فقرات التقرير آليًا من قاعدة التهيئة بالترتيب النظامي، ويستبدل المتغيّرات `#العميل#` و`#نوع الشركة#` و`#تاريخ أول الفترة#` و`#تاريخ أخرالفترة#` و`#السنة الحالية#` من ملف العميل.

**التحرير:** المجموعات متعددة الصفوف (أساس الرأي، أمر آخر، المتطلبات النظامية) تقبل «+ إضافة صف» من المكتبة أو فقرة فارغة، مع ترتيب الصفوف وحذفها.

**الاعتماد:** زر واحد يبدّل حالة التقرير بين **مسودة** و**معتمد من الشريك**. التقرير المعتمد يُقفل تمامًا ضد التعديل والحذف، وإلغاء الاعتماد من صلاحية الشريك فقط.

**الطباعة:** أوراق A4 حقيقية (210×297مم) — اكليشة المكتب أعلى كل ورقة، وفي التذييل **باركود QR صغير** يفتح صفحة تحقّق عامة (`/r/:token`) تُظهر رقم التقرير والعميل والمكتب ونوع الرأي وحالة الاعتماد، مع رقم التقرير وتاريخ الطباعة وترقيم «صفحة س من ص». المسودة تُطبع بعلامة مائية «مسودة». التوقيع اليدوي والختم يُرفعان من شاشة التقرير ويظهران في كتلة التوقيع.

### واجهات API المضافة

```
GET/POST                /api/report-config            قراءة/إضافة بنود قاعدة التهيئة
GET                     /api/report-config/summary    ملخّص المجموعات وعدد بنودها
PUT/DELETE              /api/report-config/:id        تعديل/حذف (العام يُنسخ أو يُخفى)
POST                    /api/report-config/import     استيراد دفعة بنود
GET/PUT                 /api/company                  بيانات المكتب والاكليشة والختم
GET/POST                /api/audit-reports            قائمة/إنشاء تقرير
GET/PUT/DELETE          /api/audit-reports/:id        قراءة/تعديل/حذف تقرير
GET                     /api/audit-reports/:id/library   نصوص المكتبة لمجموعة معيّنة
POST                    /api/audit-reports/:id/recompose إعادة تركيب الفقرات
POST                    /api/audit-reports/:id/approve   اعتماد
POST                    /api/audit-reports/:id/unapprove إلغاء الاعتماد (الشريك فقط)
GET                     /r/:token                     صفحة التحقق العامة للباركود
```
